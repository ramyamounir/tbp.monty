# @package _global_

defaults:
  - /monty: graph_exp1000_emin_t3_tot2500
  - /monty/motor_system_config: distant_5
  - /monty/sensor_module: camera_dist
  - /monty/connectivity: 1lm_1sm
  - /environment: habitat_dist_agent
  - /env_interface: eval_3d_env_interface
  - /env_interface/positioning_procedures_eval: getgoodview_viewfinder_patch
  - /env_interface/transform: missing_depthto3d_sensor2_semantic0
  - /logging: basic_warning_wandb_evidence_eval_runs
  - /plotter: interactive

constants:
  min_eval_steps: 5000

experiment:
  _target_: tbp.monty.frameworks.experiments.object_recognition_experiments.MontyObjectRecognitionExperiment
  config:
    max_train_steps: 1000
    max_eval_steps: 5000
    min_eval_steps: 5000
    max_total_steps: 6000
    n_train_epochs: 3
    n_eval_epochs: 1
    model_name_or_path: ${path.expanduser:${oc.env:MONTY_MODELS}/3d_sensor/learning_3d/pretrained/}
    min_lms_match: 1
    seed: 42
    supervised_lm_ids: all
    logging:
      run_name: infer_3d
    eval_env_interface_class: ${monty.class:tbp.monty.experiment.environment.OneObjectPerEpisodeInterface}
    monty_config:
      learning_modules:
        learning_module_0:
          _target_: tbp.monty.frameworks.models.evidence_matching.learning_module.EvidenceGraphLM
          hypotheses_updater_class: ${monty.class:tbp.monty.frameworks.models.evidence_matching.burst_sampling.BurstSamplingHypothesesUpdater}
          hypotheses_updater_args:
            max_nneighbors: 5
          max_match_distance: 0.001
          max_graph_size: 0.3
          num_model_voxels_per_dim: 100
          tolerances:
            patch:
              # hsv: ${np.array:[0.1, 0.2, 0.2]}
              principal_curvatures_log: ${np.ones:2}
          feature_weights:
            patch:
              # hsv: ${np.array:[1, 0.5, 0.5]}
              pose_vectors: ${np.ones:3}
              principal_curvatures_log: ${np.ones:2}
          x_percent_threshold: 20
          object_evidence_threshold: 20.0
          evidence_threshold_config: all
          gsg:
            _target_: tbp.monty.frameworks.models.goal_generation.EvidenceGoalGenerator
            goal_tolerances:
              location: 0.015
            elapsed_steps_factor: 10
            min_post_goal_success_steps: 5
            x_percent_scale_factor: 0.75
            desired_object_distance: 0.03
    environment:
      env_init_args:
        data_path: ${path.expanduser:${oc.env:MONTY_DATA}/habitat/objects/compositional_objects}
